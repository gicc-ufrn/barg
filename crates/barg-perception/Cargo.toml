[package]
name = "barg-perception"
version.workspace = true
edition.workspace = true
license.workspace = true

# Camada de percepção V4 (transdução acústico→simbólica). Sem dependências:
# roda no audio thread, só aritmética sobre buffers de canal.
[dependencies]
