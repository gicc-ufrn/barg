name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: lint + test + fronteira arquitetural
    uses: gicc-ufrn/.github/.github/workflows/mise-ci.yml@main
    with:
      antes: cargo install --locked cargo-deny
      tarefas: ci
